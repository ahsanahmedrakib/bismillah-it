import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Headsets",
  description:
    "Quality business headsets for clear calls, video meetings, and all-day comfort.",
};

const page = () => {
  return <div></div>;
};

export default page;
