import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Headsets",
  description: "Quality business headsets for clear calls, video meetings, and all-day comfort.",
  path: "/voice/hardware/headsets",
});

const page = () => {
  return <div></div>;
};

export default page;
