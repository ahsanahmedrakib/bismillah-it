import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "More Features",
  description: "Additional voice features including call recording, voicemail-to-email, IVR, and call analytics.",
  path: "/voice/features/more-features",
});

const page = () => {
  return <div></div>;
};

export default page;
