import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "More Features",
  description:
    "Additional voice features including call recording, voicemail-to-email, IVR, and call analytics.",
};

const page = () => {
  return <div></div>;
};

export default page;
