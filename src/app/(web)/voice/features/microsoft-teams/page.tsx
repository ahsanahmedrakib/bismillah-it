import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Microsoft Teams Integration",
  description: "Integrate your phone system with Microsoft Teams for seamless calling and collaboration.",
  path: "/voice/features/microsoft-teams",
});

const page = () => {
  return <div></div>;
};

export default page;
