import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Unified Communications",
  description: "Combine voice, video, messaging, and collaboration into a single platform with unified communications.",
  path: "/voice/features/unified-comms",
});

const page = () => {
  return <div></div>;
};

export default page;
