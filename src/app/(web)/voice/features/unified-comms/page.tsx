import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unified Communications",
  description:
    "Combine voice, video, messaging, and collaboration into a single platform with unified communications.",
};

const page = () => {
  return <div></div>;
};

export default page;
