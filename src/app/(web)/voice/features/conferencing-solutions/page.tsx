import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Conferencing Solutions",
  description: "Video and audio conferencing solutions for productive meetings across your organisation.",
  path: "/voice/features/conferencing-solutions",
});

const page = () => {
  return <div></div>;
};

export default page;
