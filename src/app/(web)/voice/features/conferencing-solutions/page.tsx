import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Conferencing Solutions",
  description:
    "Video and audio conferencing solutions for productive meetings across your organisation.",
};

const page = () => {
  return <div></div>;
};

export default page;
