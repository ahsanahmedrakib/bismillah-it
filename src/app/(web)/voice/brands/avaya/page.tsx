import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Avaya",
  description: "Avaya enterprise phone systems and unified communications solutions for large organisations.",
  path: "/voice/brands/avaya",
});

const page = () => {
  return <div></div>;
};

export default page;
