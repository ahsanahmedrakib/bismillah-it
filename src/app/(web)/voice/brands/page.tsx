import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Voice Brands",
  description: "Browse our range of trusted voice and phone system brands including LG iPECS, Grandstream, Vodia, and Avaya.",
  path: "/voice/brands",
});

const page = () => {
  return <div></div>;
};

export default page;
