import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Vodia",
  description:
    "Vodia PBX phone system software for flexible, cloud-ready business communications.",
};

const page = () => {
  return <div></div>;
};

export default page;
