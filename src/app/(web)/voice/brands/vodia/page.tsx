import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Vodia",
  description: "Vodia PBX phone system software for flexible, cloud-ready business communications.",
  path: "/voice/brands/vodia",
});

const page = () => {
  return <div></div>;
};

export default page;
