import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Grandstream",
  description:
    "Grandstream IP phones, PBX systems, and VoIP solutions for businesses of all sizes.",
};

const page = () => {
  return <div></div>;
};

export default page;
