import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Grandstream",
  description: "Grandstream IP phones, PBX systems, and VoIP solutions for businesses of all sizes.",
  path: "/voice/brands/grandstream",
});

const page = () => {
  return <div></div>;
};

export default page;
