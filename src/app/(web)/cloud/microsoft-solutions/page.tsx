import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Microsoft Solutions",
  description:
    "Leverage the full Microsoft ecosystem including Teams, SharePoint, Microsoft 365, Azure, Defender, and more.",
};

const page = () => {
  return <div></div>;
};

export default page;
