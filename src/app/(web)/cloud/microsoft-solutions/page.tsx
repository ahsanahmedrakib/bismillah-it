import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Microsoft Solutions",
  description: "Leverage the full Microsoft ecosystem including Teams, SharePoint, Microsoft 365, Azure, Defender, and more.",
  path: "/cloud/microsoft-solutions",
});

const page = () => {
  return <div></div>;
};

export default page;
