import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Business NBN",
  description: "Business-grade NBN plans with priority support, static IPs, and SLA guarantees.",
  path: "/internet/nbn/business",
});

const page = () => {
  return <div></div>;
};

export default page;
