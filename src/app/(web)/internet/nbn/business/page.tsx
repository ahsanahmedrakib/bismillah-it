import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Business NBN",
  description:
    "Business-grade NBN plans with priority support, static IPs, and SLA guarantees.",
};

const page = () => {
  return <div></div>;
};

export default page;
