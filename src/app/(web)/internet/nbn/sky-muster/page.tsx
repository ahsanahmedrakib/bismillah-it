import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sky Muster",
  description:
    "NBN Sky Muster satellite internet for remote and outback areas across Australia.",
};

const page = () => {
  return <div></div>;
};

export default page;
