import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Sky Muster",
  description:
    "NBN Sky Muster satellite internet for remote and outback areas accross Bangladesh.",
  path: "/internet/nbn/sky-muster",
});

const page = () => {
  return <div></div>;
};

export default page;

