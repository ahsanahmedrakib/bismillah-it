import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Fixed Wireless NBN",
  description: "NBN fixed wireless internet for regional and rural areas where fibre is not available.",
  path: "/internet/nbn/fixed-wireless",
});

const page = () => {
  return <div></div>;
};

export default page;
