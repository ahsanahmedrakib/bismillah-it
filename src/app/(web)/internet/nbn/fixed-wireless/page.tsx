import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Fixed Wireless NBN",
  description:
    "NBN fixed wireless internet for regional and rural areas where fibre is not available.",
};

const page = () => {
  return <div></div>;
};

export default page;
