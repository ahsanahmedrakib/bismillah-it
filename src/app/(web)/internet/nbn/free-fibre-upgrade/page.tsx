import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Fibre Upgrade",
  description:
    "Upgrade to faster fibre NBN at no extra cost and enjoy higher speeds and better reliability.",
};

const page = () => {
  return <div></div>;
};

export default page;
