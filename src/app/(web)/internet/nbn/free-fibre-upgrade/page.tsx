import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Free Fibre Upgrade",
  description: "Upgrade to faster fibre NBN at no extra cost and enjoy higher speeds and better reliability.",
  path: "/internet/nbn/free-fibre-upgrade",
});

const page = () => {
  return <div></div>;
};

export default page;
