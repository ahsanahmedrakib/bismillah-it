import { createMetadata } from "@/shared/lib/seo";

export const metadata = createMetadata({
  title: "Cel-Fi",
  description: "Cel-Fi signal boosters and repeaters to improve mobile coverage in poor reception areas.",
  path: "/internet/cel-fi",
});

const page = () => {
  return <div></div>;
};

export default page;
